# Copy to .env.local for local development, and add the same variables in
# Vercel → Settings → Environment Variables. See CONTACT-FORM-SETUP.md.

# Google Apps Script web-app URL (ends in /exec)
GOOGLE_SCRIPT_URL=

# Must match SHARED_SECRET in google-apps-script/Code.gs
CONTACT_FORM_SECRET=
